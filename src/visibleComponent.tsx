import React, { useState } from 'react';

import Example from './Pie';
import App from './App';

function VisibleComponent(props: number) {
    var testState = 0
    const slideNumber = props;
        return (
            <div>
            <Example width={900} height={900}/>,
            </div>

    )

}
export default VisibleComponent;
